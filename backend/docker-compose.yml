# Optional: brings up Qdrant locally for development. Comment out if you don't want Docker.
version: '3.8'
services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - '6333:6333'
    volumes:
      - qdrant-storage:/qdrant/storage

volumes:
  qdrant-storage:
    driver: local
